{{- if and .Values.rbac .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "llm-engine.fullname" . }}-role
rules:
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "llm-engine.fullname" . }}-rolebinding
subjects:
  - kind: ServiceAccount
    name: {{ if and .Values.serviceAccount .Values.serviceAccount.name }}{{ .Values.serviceAccount.name }}{{ else }}{{ include "llm-engine.fullname" . }}{{ end }}
roleRef:
  kind: Role
  name: {{ include "llm-engine.fullname" . }}-role
  apiGroup: rbac.authorization.k8s.io
{{- end }}
