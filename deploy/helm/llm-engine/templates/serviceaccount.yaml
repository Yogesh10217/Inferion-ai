{{- if and .Values.serviceAccount .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "llm-engine.fullname" .) .Values.serviceAccount.name }}
{{- end }}
